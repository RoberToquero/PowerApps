App As appinfo:
    BackEnabled: =true
    OnStart: |
        =Set(var_mes,Month(Today()));
        Set(var_año,Year(Today()));
        Set(pw_visible,false);
    StartScreen: =Login
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

